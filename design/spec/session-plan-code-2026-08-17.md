# 代码会话后续计划（2026-08-27 23:25 总导演更新，v14）

## 进度快照（相对 v13）——两项全部落地

- ✅ S0 基线固化：3 种子 × 2 局归档、确定性成立、R7 判则明确（±1 波=噪声，≥2 波漂移或胜负翻转才回查）、采集器入库
- ✅ S1 调试区整块搬迁（`cce8716`）：4,105 行逐字剪切至 `TDGameManager.Debug.cs`，主文件 19,974→**15,869 行**（-20.6%）；EditMode 51/51、基线复跑过、release 877.88MB、符号抽查 7+6 全绿；QA 独立复验 PURE PASS
- ✅ baseTint→白（`e83f40c`，grep 证实无下游消费者——兼作 12 塔扩容前数据卫生）
- HEAD = 2197a95

## 任务 1：S2 — TDRunStats 纯数据簇搬迁（P0，立即，~1 天）

运行统计/评分/遥测簇迁出主文件。协议照旧：EditMode 全量 + 基线复跑（R7 判则）+ `_build_verify.py` + 符号抽查 + QA 裁判随叫随到。

## 任务 2：S3-S6 按序推进（P0，主线）

S3 音频服务 → S4 UI 工厂 → S5 战斗服务 → S6 波次状态机（最后）→ S7 收尾至 ≤6k 行。每步同协议。注意技术设计里的裁定：**数值外置先于 S4/S6、敌人池化插 S4/S5 之间**——排到对应位置时按此执行。

## 任务 3：B.4 插桩线（P2，随 S 阶段间隙或扩充批 1 时）

设计关账把 B.4 移交给你（建造转化停滞插桩）——它已进重织输入包的修正结论（L13 主约束），插桩最自然的时机是 S5 战斗服务前后或批 1 实装 autoplay 改动时，别单独开工。

## 任务 4：阶段 2 扩充四批（不变，S7 完成后启动）

任务包细节在 v11 节。新增输入：批 3 波次重织以 `balance-reweave-input-v1.md` 为准（三档口径：L01-09 不动 / L10-15 重织验收=扩充后 meta-0 三策略通关 / L16-20 维持目标验收推迟）；批 1 对表守护设计侧已就位。

## 验收标准（不变）

每 S 阶段过完整协议；冻结完成（S7）即进扩充批 1；任何阶段 QA 叫停即刻停。
