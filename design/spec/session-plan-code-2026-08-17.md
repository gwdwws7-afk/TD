# 代码会话后续计划（2026-08-24 16:15 总导演更新，v11——内容扩充实施任务包）

## 总纲

内容扩充的设计侧已 100% 就绪（主规格 + 数值表 + Boss 相位 + 轨障契约 + 投放矩阵 + 视觉规格 + 334 资产提示词）。本计划把扩充实施完整派给你，**严格遵守主规格 §6 排期约束：冻结期先行**——四座新塔接入拆分后的结构远比拆分中途插入便宜（你自己的 R6/R8 红线）。当前发令枪状态：QA 帽后矩阵（停摆中）→ 冻结 S0-S7 → 本计划阶段 2。

## 阶段 0：立即可做（不碰冻结红线）

1. **player 构建验证债**（`_build_verify.py` 补跑 P2 批；桥仍断则与 QA 协调代跑）
2. **设计四裁定收尾批**（护甲 ceil 语义、C 线取整、增援口径、星级阈值——设计 v10 任务 1 在出）
3. **扩充契约内化 + 实施 mapping 文档**（半天，doc-only）：把四份执行级文档逐条映射到文件/单测/风险——塔表每行对应哪个数据结构与测试、轨障契约五项单测的断言草稿、Boss 相位的状态机草图、§6.4 四批的接缝。目的是冻结结束即热启动，实施期间零"读规格"成本。产出 `design/spec/expansion-impl-mapping-v1.md`

## 阶段 1：发令枪（不变）

QA 矩阵闭环 → **S0 基线固化 + S1 调试区整块搬迁**（1 天 4,100 行）→ S2-S7 按你的技术设计推进。冻结完成即进阶段 2。

## 阶段 2：扩充实施（冻结完成后，按主规格 §6.4 顺序四批）

### 批 1 · 塔 ×4 代码批（含轨障车 AI）——最重批

**连带工程（先行，半天）**：`TDTowerKind` 枚举 8→12；`TowerColors`/`TowerShortNames` 径向菜单数组、身份色表、图鉴、本地化全按 12 扩容（master spec §1 连带工程点）。

**四塔实现**（数值全以 `expansion-tower-sheets-v1.md` 为准，windup 进现有节拍锁表）：

- **塔 9 炉渣喷灯**（L04）：灼烧叠层系统（3 层起每层 2/s×3s 可叠 6）——DoT 结算建议独立纯函数进 `TDCombatMath`（灼烧跳伤吃固定减伤不吃百分比的护甲墙哲学要用单测钉死）；双专精 Slag Sump（满层爆发）/ Wildfire Drift（击杀传染 2 格 ≤3 目标）
- **塔 10 捞轨吊机**（L09）：击杀赏金光环（2.5 格 +18% 起）+ 打捞线保底金；**单波收入 ≤45 平衡红线必须有单测守护**（与 p12.5.0 经济门联算）
- **塔 11 轨障车**（L14）：按 `rail-barricade-behavior-spec-v1.md` 契约全量实施，预估 ~300-400 行：
  - 拦截车体独立实体（**独立文件**，如 `TDBlockerWagon.cs`——meta 先例红线）；敌人状态机新增 `Engaged` 态（Moving↔Engaged，寻路不改）
  - 接战容量 ≤2 排队、1.2s 接战攻击（复用 lineDamage）、25s 重建（Holding Order 15s）
  - 绕行者清单硬编码进守卫：掘地工兵潜行、滑翔者飞越、**Boss 碾压一击毁车但停 3s**、灰烬虫群每 4 漏 1
  - 五项系统交互逐条落地：共振零耦合、场景技叠加、autoplay 出售守卫（接战数=0 才可卖 + 车体豁免零贡献口径防误卖）、波清空判定（接战者不计清空）、车体不存档
  - **五项单测**（契约 §5）：接战容量、绕行者清单、Boss 碾压、波清空判定、autoplay 出售守卫
- **塔 12 远程轨道炮**（L18）：4.8 格最远射程 + 直线穿透（衰减 0.7/目标）+ 蓄力 0.5s；30% 快速敌闪避弱点与 Ballistic Lead 专精消闪避——与 FrostCoil/减速的协同闭环按表实现

每塔过完整验证协议（EditMode + `_build_verify` + autoplay 3 种子×2 局中位数基线）。设计符合性对表（其 v10 任务 4）。

### 批 2 · 敌人 ×6 + Boss ×4 行为批

- **六新敌行为**（投放矩阵 v2 基线，均为现有系统扩展）：灰渣傀儡死亡余烬堆（踩过 +25% 速）、轨面裂虫直/弯道变速 ×1.8/×0.7、酸蚀孢囊死亡喷酸降塔攻速（首个打击塔位的敌人）、锻炉骑兵每波前 3 次命中免疫盾（荆棘反伤对其无效的克制保持）、余烬行者被标记 +25% 易伤且无法闪避、回响虫群死亡复制最近死亡敌（50% 血）
- **四 Boss 相位**（`boss-design-spec-v1.md`）：装卸兽（50% 卸甲狂奔 + 周期抛集装箱封建造格 10s）、岔道暴君（15s 强制改道 + 35% 血分裂双体分裂瞬间清 debuff）、窑炉看守（每 10s 叠甲上限 +8 唯 Kiln Purge 可削）、回响先驱（拟态本场建造最多塔种 + 70/35 相位重拟态清 debuff，六类拟态行为表）
- 连带：独立 Boss 警告 FX 钩子（`fx_boss_warning_{kind}` 命名契约）、相位提示 UI 复用现有 Boss 相位观察、掉残渣 ×3 系数（若 meta 已接线）

### 批 3 · 波次重织

20 个波次文件按 `content-matrix-20-level-v2.md` 重排：新敌首现波纯净、Boss 波插入 L05/09/13/17 的 wave 18-20 段、复现率目标（爬虫/虫群 90%→≤55%）落地。语法工具链现成，纯数据批。

### 批 4 · 全矩阵重校（QA 主导、你支援）

20 关 × 3 难度 × 3 策略 + 焦点局；保留判据：单塔种胜利=0、**meta-0 自洽**、Siege/Grav 出场与零贡献不恶化；五考试关（Boss 化）重点重校。结果回流设计的反制网矩阵表更新与 GDD 回写。

## 红线（全程）

- 新系统独立文件优先（轨障车/灼烧结算/拟态系统）；TDGameManager 主文件只允许 §1 连带工程点的最小扩容
- 每批独立提交、完整验证协议、设计对表 + GDD 回写（设计侧）+ QA 回归三方闭环
- §7 开放决策已由总导演裁定（第 5 槽/难度上浮/亲和再平衡 → 推迟扩充后数据），实施中不预做

## 验收标准

- 阶段 0 三项清零（含实施 mapping 文档）
- 四批按序落地，每批单测齐、验证全绿、三方闭环
- 批 4 判据全过 → 内容扩充代码侧关账
