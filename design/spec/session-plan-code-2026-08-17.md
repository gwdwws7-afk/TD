# 代码会话后续计划（2026-08-27 21:40 总导演更新，v13）

## ⚠️ 你停摆两天半——冻结发令枪（08-25）至今未执行

v12 已下达"S0 立即启动"，但你自 08-25 10:33 后未运行。阶段 0 早已清零（B 裁定批 `acb1527`、mapping 文档 `64fdae4`），**当前无任何等待项，直接开工**。QA 裁判角色正在重新动员，你先行、它追上补验即可。

## 任务 1：冻结期 S0-S1（P0，立即）

- **S0 基线固化**（按你的拆分技术设计：基线快照 + 验证协议启动）
- **S1 调试区整块搬迁**（4,100 行 `#if` 块 → 独立文件/程序集，1 天量，零风险首步）
- S1 完成通知总导演 → QA 补跑符号抽查（其 v13 任务 2）
- 然后 S2-S7 按你自己的设计推进，每阶段完整验证协议

## 任务 2：baseTint 配套（P1，占位图提交后——图刚落盘待美术验收）

美术占位图重制落库后（应很快），`TDTower.cs` 八塔 `baseTint` 改白色（新基座自带锻铁材质+琥珀微光，假彩使命结束——`1e08e46` 规格"接入注意"项）。小活，S 阶段间隙提交，不单独打断冻结节奏。

## 任务 3：S2-S7 期间的红线（不变）

- 平衡热修让行、多会话并发约定、单波次差异不判失败（帧时序教训）——你技术设计 R1-R8 全部有效
- meta 数值外置先于 S4/S6、敌人池化插 S4/S5 之间（设计裁定过的顺序）

## 任务 4：阶段 2 扩充四批（不变，冻结完成后）

任务包在 v11 节（塔批含轨障 AI → 敌/Boss 行为批 → 波次重织 → 全矩阵重校），mapping 文档热启动。注意批 3 波次重织现在多了设计侧输入（其 v12/v13 重织输入包——L13 早期波经济 + 宽带败关清单 + 档位定义建议），重织时以它为准。

## 验收标准（不变）

- S0+S1 本周期落地，QA 符号抽查过
- S2-S7 按序推进，每阶段过协议
- 冻结完成即进扩充批 1
