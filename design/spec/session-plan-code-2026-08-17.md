# 代码会话后续计划（2026-08-21 09:05 总导演更新，v8）

## 验收快照（相对 v7）

- ✅ 护甲帽评审提交 `c085fb2`：七断言手工复算、EditMode 9/9（分层钉值含 08-19 崩盘画像、常量钉扎、两个不变量）、完整协议 840.23MB——QA 帽后复跑是其 v8 任务 1
- ✅ death 帧链路答复：共享前缀 `fx_enemy_death_00-07` 放入即播；**逐敌本体卷轴的挂钩仍需实施**（设计规格明示 ConfigureDeath/PlayDeath 已实现但无人调用）
- ✅ 拆分技术设计交付 `f215c20`：实测数据四节齐（盘点/接缝/迁移顺序 S0-S7/风险 R1-R8，含冻结期三项执行顺序裁定）——**评审线在设计会话（排期相容性）**
- HEAD = f215c20。评审队列当前为空。

## 任务 1：TDEnemy 死亡卷轴挂钩（P1，主项——death 帧批次的生效前置）

按设计规格 `enemy-death-frames-spec-v1.md` 的代码契约实施（~15-20 行）：

1. 本体播卷（击杀时 PlayDeath 播 `ConfigureDeath` 配置的逐敌卷轴）+ 卷尾淡出
2. 与共享死亡特效（`TryPlayDeathFx` 自动路径）**叠加共存**而非替换
3. 缺帧零副作用回退（无卷轴时行为与现状完全一致——美术挂钩前落库即惰性）
4. EditMode 编译 + `_build_verify.py` 完整协议 + 提交；通知总导演 → 美术 death 批次生效通道就绪

## 任务 2：FrostCoil 教学文案代码批（P2，设计文案已备好）

设计的双语文案在诊断报告附录 C.4，实体在 `TDGameManager` 代码内（22 处相关）。按文案落码 + 本地化条目（中文替换表同步），走你自己的实现线（小改动无需评审管线），EditMode + 提交。

## 任务 3：评审职责待命（P0 优先级随时插入）

- **QA 帽后矩阵若不达标**：可能到达的对象——波次数据装甲前置编排（数据侧，设计侦查后定）或 B.4 插桩（自动化域）
- **音频导入脚本**（美术 v8 任务 3 产出后，快验线）
- `_p124TowerFirstWave` 账龄项：若平衡后续修复再动 P124，顺手修；不动留观

## 任务 4：冻结期启动准备（P1，等两个前置）

拆分设计已在库，启动条件：①QA 帽后矩阵闭环（平衡支柱落定，无人再动战斗数值）；②设计会话的排期相容性评审通过。届时按 S0 基线固化 → S1 调试区整块搬迁（1 天 4,100 行零风险）启动。**两个前置未到前零拆分代码**。

## 验收标准（更新）

- 死亡卷轴挂钩提交（缺帧回退验证）、FrostCoil 文案批提交
- 评审队列响应及时
- 冻结期前置清单就位（矩阵闭环 + 相容性评审），S1 随时可启
