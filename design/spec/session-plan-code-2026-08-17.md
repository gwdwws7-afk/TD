# 代码会话后续计划（2026-08-20 12:45 总导演更新，v7）

## 验收快照（相对 v6）——v6 三项全部完成

- ✅ 配额修复评审：`4bb5480`——根因核验属实（"配额在场、成员资格为零"），修复数据驱动零硬编码
- ✅ TD-GP-004 修复：`f959401`——教程面板图形退出射线检测，按钮保留，步骤门控不变
- ✅ 对账表实现侧：⚙1 世界地图背景已修（`8a1dd56`，图复制入 Resources、原图不动）；⚙2 en 键名治理降级冻结期（理由入库）；其余维持
- 三笔均过完整协议，工作区代码文件清零，HEAD = 8a1dd56
- QA 终验工件已产出；配额修复的矩阵验证在 QA 手里跑——**你的评审队列当前为空**

## 项目位置判断（总导演评估）

功能开发已全部收口（美术/手柄/发布三支柱绿，平衡在 QA 复跑中）。冻结期主项的启动条件是"平衡闭环 + 无人在途改动"——现在只差 QA 那一轮矩阵。** preparatory 工作可以先行，代码本体冻结等平衡闭环**。

## 任务 1：GameManager 拆分技术设计（P1，主项——只写文档不动代码）

对账表排期建议的冻结期主项，你来做技术侧设计（设计会话已给排期侧输入）：

1. **盘点**：调试区 ~4000 行 `#if` 块的边界现状（P3 门控后是否仍整体可搬迁）、主文件 19,600 行的职责聚类（WaveDirector / EconomyService / UiFactory / 战斗编排等）
2. **接缝清单**：每个目标模块的输入输出、与主文件的调用边、需要先立的接口
3. **迁移顺序**：按"解耦模式复制"原则排（先纯函数/数据类、后行为类），每步的验证协议（EditMode + `_build_verify.py` + autoplay 基线）
4. **风险登记**：与数值外置、敌人池化的耦合点（对账表冻结期三项的执行顺序建议）
5. 产出：`design/spec/gamemanager-split-tech-design.md`，评审线：设计会话过一遍排期相容性

**红线**：本任务零代码改动。QA 矩阵结果出来前不启动任何拆分实施。

## 任务 2：评审职责待命（P0 优先级随时插入）

- **护甲帽**：若 QA 矩阵不达标解封——管线已备（公式核验 + windup 模式单测 + 双编译）
- **B.4 插桩**：若 focused 局停滞触发设计插桩需求——插桩代码属自动化域，评审快过
- 设计的 death 帧规格若需要代码挂钩（其 v7 任务 3 会来找你确认生效链路）——如实答复"放入即播"还是要挂钩

## 任务 3：小项扫尾（P2，可选填充）

- 音频导入脚本的**代码侧评审**（若美术 v7 做了）——纯 Editor 工具，快验
- `_p124TowerFirstWave` 账龄观察项（你上轮入库的）：若 QA 矩阵后 P124 还要动，顺手修；不动就留观

## 验收标准（更新）

- 拆分技术设计文档交付（盘点/接缝/顺序/风险四节齐）
- 评审队列响应（护甲帽/插桩/death 帧挂钩任一到达即评审）
- 本周期内零代码提交是正常状态——你在为冻结期铺路，不是怠工
