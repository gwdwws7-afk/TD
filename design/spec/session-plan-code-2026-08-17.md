# 代码会话后续计划（2026-08-21 14:40 总导演更新，v9）

## 进度快照（相对 v8）

- 你在途的文件已见（TDEnemy/TDGameManager/TDTowerTooltip 修改中）——按 v8 任务 1/2 继续（死亡挂钩 + FrostCoil 文案），完成后照常提交
- 新需求到达：世界地图结构化重做（规格 v2 `159bafd`），**代码挂钩从 ~40 行涨到 ~120-150 行**（锚点表、地标+徽章+印章节点组合渲染、删章节带换区域名牌、轨道双色 tint）——契约在规格里
- **局外升级系统已立项**：设计出规格（其 v9 任务 1），你实现（见任务 3，**独立文件红线**）
- HEAD = 159bafd。QA 帽后矩阵复跑仍是全局关键路径（与你无关但在途）。

## 任务 1：收口 v8 在途（P0，先做完手上的）

死亡卷轴挂钩（缺帧回退验证）+ FrostCoil 文案批——两笔各自提交，通知总导演。别让新需求打断收尾。

## 任务 2：worldmap 挂钩（P1，等美术第一波世界图验收后）

按 v2 规格契约实施（~120-150 行，`TDWorldMap.cs` 侧）：

- 锚点表（20 关坐标 → 地貌区）
- 节点组合渲染：地标立绘 + 徽章 + 三枚难度印章（读 `highestDifficultyCleared`）+ 编号
- 区域名牌替换章节带、发光轨道 cleared/locked 双色（复用现有 tint）
- 局外升级入口按钮挂接（任务 3 的面板出来前先占位禁用态）
- 验证：EditMode + `_build_verify.py` + 提交；与设计的信息设计复核（其 v9 任务 2）对表

**注意**：TDWorldMap 改动与你自己的拆分设计 S 阶段有交集——挂钩先落库再谈拆分，别并行。设计会话相容性评审时会核这条。

## 任务 3：局外升级系统实现（P1，等设计规格——立项已批）

**红线：独立文件**（建议 `TDMetaProgression.cs` + 存档扩展），不许往 TDGameManager 长一行——与拆分方向一致，设计规格的四道护栏（MVP/货币/存档/**meta-0 平衡基线**）是验收线。实现后交 QA 验证（其计划已挂平衡红线核验）。

## 任务 4：评审职责待命（不变）

B.4 插桩、音频导入脚本（美术 v9 任务 4 产出后快验）、波次数据调整（若 L13 线触发）。

## 任务 5：冻结期启动前置（不变，且新增核对项）

矩阵闭环 + 设计相容性评审——评审会核对 worldmap/meta 两条新功能线与 S 阶段的隔离。两个前置到齐前零拆分代码。

## 验收标准（更新）

- v8 两笔在途提交落地
- worldmap 挂钩提交并与设计复核对表
- meta 系统按规格实现（独立文件 + meta-0 基线守护）
- 每笔完整验证协议
