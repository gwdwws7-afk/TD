# QA 交接清单 — B103 实机验证（2026-08-17，美术会话下发）

> 背景：美术会话无法连接 Unity MCP（工具断连），实机步骤移交 QA。
> B103 已提交（`6308032`）。修复内容：TDArtBatch103Import.cs 的 CS0029 编译错误
> （`const string PPU_TARGET = 1024` → `const int`），39 张新图 meta 已手工对齐
> PPU 1024 + 库标准压缩（默认 512 压缩 / Standalone 512 + DXT5）。
> 根因记录：8/17 12:59 Unity 刷新导入了 39 张新图，但后处理器因编译错误未注册，
> 图以 PPU=100 导入——贴花走固定 localScale 缩放（TDGridMap.cs:650 附近），
> 会放大 10.24 倍；设备/道具按 bounds 归一无碍。

## 步骤（按序执行）

1. **Focus Unity 编辑器**，等待重编译。确认 Console 无编译错误
   （此前 Assembly-CSharp-Editor 一直处于失败状态）。
2. **跑菜单 `TD/Art/Reimport Batch 103`**（兜底重导入，~130 张，日志应有
   `[TDArt103] Reimported ...`）。
3. **抽查 Inspector**：`decal_hollow_kiln_basin_path_a` 与 `device_reserve_train`
   的 Pixels Per Unit = 1024。
4. **进 Play 模式开新档（L1 grayline_junction）**：
   - 地图上贴花/道具尺寸正常（无巨幅贴片糊图、无悬浮方块）；
   - 实机评估 `decal_hollow_kiln_basin_path_a`（57% 不透明大色块）在
     hollow_kiln_basin 图上是否读作"路径贴片"——结论落档回美术会话。
5. **考试设备**：Play 模式调用 `TDGameManager.DebugPrepareP122ExamForTest()`
   （TDGameManager.cs:18593），确认设备不再是信号柱占位图，光环/充能点正常。
6. **FX 抽查**：刷 `husk_titan` 看 boss_warning 10 帧（12FPS，金→红渐隐）；
   `burrow_sapper` 出土看 burrow_ambush；`spore_carrier` 打至低血看分裂预警。
7. **内存 sanity**：重导入后 Profiler 只读纹理应比修复前（未压缩 RGBA32）低
   ~100MB+。

## 已知非阻塞项

- 3 张建议重跑（外部生图模型）：`prop_split_switch_canyon_a`（4.8% 内容 +
  44% 薄雾）、`prop_last_ember_terminus_a/_b`（~7% 偏空）。重跑后走同一导入流程。
- `game_logo.png` / `logo_chain.png` / `main_menu_bg.png` 仍是假 PNG（JPG 内容），
  修复排在 backlog。
