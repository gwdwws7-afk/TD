# QA 会话后续计划（2026-08-27 23:25 总导演更新，v14）

## 进度快照（相对 v13）——四项完成（`2197a95`）

- ✅ 平衡支柱终版记录：结构性关账落档、八次平衡事件全追溯
- ✅ S0+S1 裁判：符号抽查 PURE PASS（纯包 172MB、自动化符号 8/8 剥离、玩法 9/9 在包）、EditMode 51/51、六局对照 +1 波正向漂移判明非回退（新锚点已记录）
- ✅ 占位图 + baseTint 实机复看过——用户圈选问题**关账**
- ✅ 计划文档入库、SellRefundValue 谱系澄清（meta P2 有意移除）
- HEAD = 2197a95。代码进 S2。

## 任务 1：S2+ 各阶段裁判（P0，主项，随叫随到）

S2（TDRunStats）即将落库，照 S1 协议：EditMode 全量、六局基线对照（用 S1 后的新锚点）、纯包符号抽查（重点：运行统计/遥测搬迁后自动化域归属不漂）。之后 S3-S6 每阶段同协议。

## 任务 2：扩充批 4 矩阵工具预备（P1，冻结空档做）

20 关 × 3 难度 × 3 策略 + 焦点局跑法确认；判据清单按 `balance-reweave-input-v1.md` 三档口径落档（L01-09 对照旧档 / L10-15 终局跑验收 / L16-20 推迟）；改进幅度报告格式定义（对照冻结基线）。

## 任务 3：worldmap 剩余三项（P2，等美术资产全落地后）

难度印章数据绑定、区域名牌、轨道亮暗——与 C-2/C-3 资产复验合并做。

## 任务 4：_audit 清整（P2，收尾）

`_audit/`、`_audit_shot.jpg`：引用它们的文档已入库，按引用需要决定去留——留档就入库，不留就删。这是工作区最后两件散件（用户 PDF 除外）。

## 约束（不变）

不改游戏逻辑代码；多种子判定；提交只含 QA 产物。
