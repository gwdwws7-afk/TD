# QA 会话后续计划（2026-08-24 14:35 总导演更新，v10）

## ⚠️ 你停摆 4 天（08-20 起），是全局唯一瓶颈

你缺席期间项目大幅推进，你的任务清单不是缩小而是变长了。以下按解锁顺序排列，**任务 1 现在就能做且堵着三条下游线**。

## 你缺席期间发生的事（HEAD = 32cf33c）

- 护甲帽 `c085fb2` 落库后**帽后矩阵一直没人复跑**——它堵着：失衡诊断关账、冻结期启动（前置一）、内容扩充的对照基线
- 代码会话完成第二轮全面审查：P0×6 + P1×4 + P2×16 全部清零（`6307a31`/`db41ff8`/`7453572`）——含波次 5 秒后门门控、音频导入加固、死亡表现修复、世界图性能、meta 系统多项实现（EmberResidue 货币、rank 校验和等）
- worldmap 已接线（`6b61a39`）+ 美术资产推进中；内容扩充主规格 + 派生规格批（12 塔/18+5 敌/4 Boss/轨障车契约）全部就绪
- 代码有一笔验证债：桥接宕机期间只做了三路离线编译（全绿），**player 构建未跑**——你有 MCP，可代跑或催办

## 任务 1：护甲帽矩阵复跑（P0，立即——堵三线的全局关键路径）

协议同 v8：L13/L20 各 ≥3 种子 × 5 局 + 25 局门禁同批。判据树不变（达标→失衡关账+冻结前置一闭环；L13 早期波→波次侦查线；focused 停滞→B.4 插桩）。**这次结果还多一个身份：内容扩充的"扩充前对照基线"**——数据留档要完整（逐局 JSON 按你 v7 格式），扩充后全矩阵重校要拿它对照。

## 任务 2：第二轮审查修复的回归批（P1，任务 1 后）

26 项修复需要回归确认，重点抽验：

- **波次 5 秒后门门控**：release 构建（默认路径）标题屏不再 5 秒强制部署、不刷 warning；自动化域行为不变
- **死亡表现**：单帧行死亡卷轴不吃 0.75s 保险、尸体不再卡纯白态（美术 death 帧入库后一并看）
- **世界图性能与回退**：地标缓存生效、缺图回退为着色形状非白块
- **音频导入加固**：第三方音频不再被强改单声道（抽查一个非管理路径资产）
- EditMode 全量 + 一局 playtest

## 任务 3：meta 系统红线核验（P1，已实现部分的验收）

代码已实现 EmberResidue 货币、rank 归一化、B 线/D 线等（细节见其 P2 报告）。核验四护栏中的红线：**meta-0 基线**——零升级下 L01-L20 难度档位与任务 1 结论一致；升级效果不构成通关门槛。

## 任务 4：worldmap 实机验证（P2，资产全部落地后）

三枚难度印章数据绑定（抽 3 关对 `highestDifficultyCleared`）、区域名牌、轨道亮暗与进度一致、局外升级入口按钮状态、性能（选中环脉冲已修——复核）。

## 任务 5：player 构建验证债（P2，与代码会话协调）

代码会话桥接宕机欠的 `_build_verify.py` player 构建——谁的 MCP 先恢复谁跑，结果共享。

## 约束（不变）

不改游戏逻辑代码；多种子判定；提交只含 QA 产物。
