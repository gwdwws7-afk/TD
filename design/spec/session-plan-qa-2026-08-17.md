# QA 会话后续计划（2026-08-20 12:45 总导演更新，v7）

## 验收快照（相对 v6）

- ✅ 任务 1 手柄终验工件达成：纯手柄 L01 主动开波 **20/20 通关**（P124 决策轨迹 × 纯 InputSystem 事件，零调试 API 改状态，主动开波路径全程）——工件三件已落档（`57af610`）。**已转设计回填 P12.3**（其 v7 任务 1）
- ✅ 任务 4 杂项清整：jobid.txt 删除、_windup_ab.py 与协调文档入库
- 上游全部就位：配额修复 `4bb5480`（代码已评审）、TD-GP-004 修复 `f959401`、世界地图背景修复 `8a1dd56`
- 四支柱三绿：美术 ✅ / 手柄 ✅ / 发布 checklist ✅——**只剩平衡，而你的复跑就是它的最后一环**
- HEAD = 8a1dd56

## 任务 1：平衡矩阵多种子复跑（P0，全局关键路径，立即——所有前置已落地）

按设计的判据（诊断报告附录 B.5）+ 你自己的多种子要求执行：

1. **L13/L20 各 ≥3 种子 × 5 局** + 25 局门禁同批（RailLancer 单塔种 = 0 底线不破）
2. **编队判据**：adaptive/control 局的编队应含 SiegeDrill（配额修复的直接验证——它终于该上场了）
3. **B.4 分流规则（重要）**：若 focused 局仍 3-4 塔停滞，**别归因配额**——那是设计归档的次级异常（建塔循环停滞、死亡时未花预算 348-1254），触发条件已写入判据，按设计留的路径上报插桩需求
4. GravSnare 观察：若仍"从不被选"，如实记录（选择策略问题，非数值）
5. 结果三向通知：达标 → 设计关账失衡诊断 + 你汇总四支柱；不达标 → 总导演决策（护甲帽解封评审线已就位）；focused 停滞 → B.4 插桩线

## 任务 2：TD-GP-004 复验（P1，立即可做，可与任务 1 并行）

`f959401` 的修法是教程面板图形退出射线检测（按钮保留）。复验：教程期内 (8,1) 塔位指针可达可建造、教程步骤门控语义不变（穿透建造仍受步骤约束）、鼠标与手柄光标双路径。

## 任务 3：项目级关账报告（P0，任务 1 之后——本周期收口动作）

四支柱终态汇总给总导演出项目级结论：

1. 美术缺口清零 ✅（已关账）
2. 手柄 P12.3 终态 ✅（工件 + 终验文档）
3. 平衡回档（任务 1 结果）
4. 发布 checklist ✅

附：对账表"等 QA 2 项"闭环确认（P12.3 回填、失衡关账）+ 遗留清单（B.4 或focused 停滞、GravSnare 选择问题、假 PNG logo、en 键名冻结期项）。这是本协作周期的收官交付物。

## 约束（不变）

不改游戏逻辑代码；单种子结论不采信（帧计时脆弱性已实证）；提交只含 QA 产物。
