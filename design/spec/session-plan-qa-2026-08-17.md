# QA 会话后续计划（2026-08-27 21:40 总导演更新，v13）

## ⚠️ 你停摆两天半（08-25 10:18 起）——v12 任务未启动

v12 的四项任务原样有效，另有一项新到验证（占位图）。代码会话正在被重新动员进 S0/S1，你追上补验即可。

## 任务 1：平衡关账终版记录（P0，半小时，立即）

总导演裁定（08-25）落档：帽后矩阵"结构性关账——被内容扩充波次重织接管"；宽带败关清单（L10/11/14/15/16/19）移交设计作重织输入；B.4 与波次侦查线标记"随扩充实施消解"。这是"平衡"支柱的周期终态记录。

## 任务 2：冻结期裁判（P0，主项——代码 S0-S1 落库后追上补验）

- S1（调试区搬迁 4,100 行）后**必跑符号抽查**：release 包内调试域符号零变化（剥离面回退即叫停）
- 每阶段：EditMode 全量（基线 50/50）+ 3 种子 × 2 局 autoplay 中位数对照冻结基线（63 文件档就是你冻的）
- 之后每个 S 阶段同协议随叫随到

## 任务 3：占位图实机复看（P1，新到——美术验收提交后）

两张地面占位图重制（用户圈选的灰盘问题）：实机确认新基座+建造标记观感、八塔 tint 不再洗灰（baseTint→白的配套改动落地后一并看）。通过即用户圈选问题关账。

## 任务 4：worldmap 剩余三项（P1，等美术资产全部落地后）

难度印章数据绑定、区域名牌、轨道亮暗——一轮补验。

## 任务 5：扩充批 4 矩阵工具预备（P2，冻结期空档）

20 关 × 3 难度 × 3 策略 + 焦点局跑法确认、判据清单落档（含"对照冻结基线的改进幅度报告格式"）。

## 任务 6：清整（P2，顺手）

`_audit/`、`_audit_shot.jpg` 结论落档后删或入库；下次提交把四份会话计划的未提交改动（总导演 v12/v13 更新）一并入库可追溯。

## 约束（不变）

不改游戏逻辑代码；多种子判定；提交只含 QA 产物。
