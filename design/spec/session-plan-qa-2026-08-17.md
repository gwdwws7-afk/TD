# QA 会话后续计划（2026-08-21 09:05 总导演更新，v8）

## 验收快照（相对 v7）

- ✅ 任务 1 多种子矩阵：种子间完全一致（节拍修复的确定性经多种子验证）；**配额修复在目标处完全生效**（L20 SiegeDrill 进编队 3/3、GravSnare 被 control 拾取、L20 adaptive 从 10-16 波升到稳定 18 波）；门禁守住（RailLancer 单塔种=0）；**L13 0/15、L20 0/15 未回档** → 护甲帽按判据解封
- ✅ 任务 2 TD-GP-004 复验 PASS（塔位射线 0 命中、手柄建造成功）
- ✅ 任务 3 项目级关账报告落档（`86381fb`）：四支柱三绿一待（平衡）；QA 周期 v3–v7 收官（6 bug 闭环、3 次判决性 A/B、2 件纯手柄工件、55+ 局矩阵）
- 上游已动：护甲帽经代码评审落库 `c085fb2`（EditMode 9/9、七断言手工复算、840.23MB 构建）——**帽后矩阵复跑是当前全局关键路径，还没人跑**
- 美术实机修正已落库 `139eb83`（beacon 光晕收紧 + death 消散雾清理 + 344 帧全目录清扫）；T2 44 帧待补 4 帧后入库
- HEAD = f215c20

## 任务 1：护甲帽矩阵复跑（P0，全局关键路径，立即——`c085fb2` 已落库）

同 v7 协议：L13/L20 各 ≥3 种子 × 5 局 + 25 局门禁同批。判据树：

- **达标**（L13/L20 回难度档）→ 通知设计：失衡诊断正式关账（对账表最后一项闭环）+ 你更新关账报告的平衡支柱为 ✅——四支柱全绿，项目级关账完成
- **L13 仍死于早期波**（设计的预判：L13 早期波非装甲主导，死在装甲波到来之前）→ 结果直报总导演，下一线是**波次数据的装甲波前置编排**（数据侧侦查，设计已登记）
- **focused 局停滞照旧** → 预期行为（B.4 独立线），触发插桩需求给设计/代码
- 附带核验：帽后轻甲带行为不变（设计承诺"轻甲完全不变"）、单塔门禁不破、SiegeDrill/GravSnare 出场维持

## 任务 2：美术实机复验批（P1，等美术 T2 48 帧入库后一次做）

1. **T2 三档读感**：T1→T2→T3 两次跳变各自可辨、T2 不抢 T3 跳变感、等级 pip 不被遮挡
2. **实机观感跟进**（你上轮截图圈的两个问题）：beacon 光晕收敛后读感、death 消散雾清理后的死亡反馈观感
3. 回退路径抽验：缺帧塔保持基础外观（T2→基础回退，挂钩 `dcc649c` 的逐级逻辑）

## 任务 3：death 帧生效验证（P2，等美术 death 批次 + 代码挂钩落地后）

设计规格（12 敌 × 4 帧）+ 代码挂钩（TDEnemy 侧）就位后：击杀播放本体死亡卷轴 + 卷尾淡出 + 共享死亡特效叠加 + 缺帧零副作用回退。并入常规回归。

## 任务 4：关账报告终版（P2，任务 1 结果后）

平衡支柱终态写入 `qa-cycle-closure-report-2026-08-20.md`（或续篇），遗留清单刷新（GravSnare 选择策略、假 PNG、B.4、编辑器运维项、en 键名冻结期项）。若四支柱全绿，给总导演的周期终报就绪。

## 约束（不变）

不改游戏逻辑代码；多种子判定；提交只含 QA 产物。工作区的 `_audit_shot.jpg` 是你的取证截图——结论落档后删或入库，别挂着。
