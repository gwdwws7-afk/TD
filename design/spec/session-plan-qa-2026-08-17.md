# QA 会话后续计划（2026-08-19 11:00 总导演更新，v6）

## 进度快照（相对 v5）——v5 全部完成，两笔提交

- ✅ hollow_kiln 复看 PASS → **全部美术缺口正式清零**（`4edf196`，台账落档）
- ✅ TD-WINDUP-001 修复复验 PASS：EditMode 35/35、autoplay 三策略 × L01 全 20/20 零逃（超 8/17 基线），并诚实更正了此前漏传 strategy 参数的假警报
- ✅ 手柄终验输入面达标；平衡矩阵复跑：门禁 PASS、L01/L05/L09 回档 5/5，**L13/L20 仍 0/5**（SiegeDrill 零出场、GravSnare 仅 1 选），解封护甲帽
- ✅ P12.3 终态表 + 发布 checklist 完备（`5dc6595`）
- 新开 TD-GP-004（P3）：教程面板遮盖 (8,1) 塔位
- HEAD = 5dc6595。**总导演调度决定：平衡侧先查"配额为何零生效"再动护甲帽**（设计 v6 任务 1），你的矩阵复跑排在其后。

## 任务 1：手柄终验工件——"P124 决策 × 手柄事件"适配器（P0，立即可做，不依赖任何人）

你自己定性了：约 10 次尝试止步 7-15 波是驱动策略在 MCP 时延下吞吐不足，非输入系统问题。按你的建议做适配器：

- P124 autoplay AI 负责决策（选址/升级/开波），执行层翻译成手柄事件（走你的 `qa_gamepad_acceptance.py` 既有事件通道，勿用调试 API 直改状态——保持"手柄路径真实被测"的性质）
- 产出"主动开波 + 通关"单局工件，补齐手柄终验最后一块
- 工件落地 → 通知总导演转设计回填 P12.3 终态
- 注意 P124 属自动化门控域：适配器跑在编辑器/开发构建（TD_AUTOMATION）下没问题，工件结论注明环境

## 任务 2：平衡矩阵复跑（P0，等设计 v6 任务 2 落地后——顺序别乱）

前置：设计的配额定位（其任务 1）+ 按结论的修复/护甲帽（其任务 2，经代码评审提交）。然后：

- **多种子取样**（代码会话交接提醒：单种子判定天然脆弱）——L13/L20 每图 ≥3 种子 × 5 局，25 局门禁同批
- 判据：L13/L20 回难度档；SiegeDrill/GravSnare 出场率与零贡献率对照诊断报告 A.4 改善
- 达标 → 通知设计关账失衡诊断；不达标 → 结果直报总导演（可能需要双管齐下或重审方案）
- GravSnare 侧留意：QA 数据显示"被选时有效但从不被选"——若配额修复后仍不被选，这是选择策略问题不是数值问题，如实记录别硬调

## 任务 3：TD-GP-004 修复复验（P2，等代码会话修复后）

教程面板遮盖 (8,1) 塔位——修复后复验：教程期内该塔位可建造（指针可达且不被面板遮挡）。P3 级，跟随下一批代码提交一起验即可。

## 任务 4：杂项清整（P2，10 分钟级，顺手做）

- `jobid.txt`（仓库根，某次工具运行遗留）——确认无用后删除
- `tools/_windup_ab.py`（你的 A/B 脚本，未提交）——补提交入库（TD-WINDUP-001 的判决性工具，值得留档）
- 协调文档（session-plan-*、qa-handover-*）已被提交消息引用多次——建议一笔入库做追溯（总导演文档，你提交时注明来源即可）

## 任务 5：项目级关账供料（P2，全部上游就绪后）

四大支柱状态汇总给总导演出项目级结论：美术缺口清零 ✅ / 手柄终验（工件落地后）✅ / 平衡回档（任务 2 后）/ 发布 checklist ✅。附：对账表"等 QA 2 项"（P12.3 回填、失衡关账）的闭环确认。

## 约束（不变）

不改游戏逻辑代码；发现 bug 报总导演分派；提交只含 QA 产物。平衡复跑严格排在设计任务 2 落地之后。
