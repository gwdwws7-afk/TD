# 设计会话后续计划（2026-08-21 09:05 总导演更新，v8）

## 验收快照（相对 v7）——v7 五项全部交付

- ✅ P12.3 终态回填：手柄四件套全绿、TD-GP 001-004 带提交号、工件注脚与环境限定——**文档与实况零偏差**，对账表"等 QA"项之一闭环
- ✅ 护甲帽按判据解封实施：`min(flat, ceil(postPercent×0.5))` + 单测全套（重甲期望改写/FlatShareCap 专项/单调性/常量钉扎），经代码评审落库 `c085fb2`——QA 帽后复跑是其 v8 任务 1
- ✅ death 帧规格交付（`enemy-death-frames-spec-v1.md`，12 敌 × 4 帧 + 代码契约明示）
- ✅ Boss 立绘需求交付（`boss-portrait-requirements-v1.md`）
- ✅ FrostCoil 双语教学文案备好（附录 C.4，落码在代码会话 v8 任务 2）
- 你的矩阵结论（附录 3）：配额修复在目标处验证通过；残留两机制——L13 早期波非装甲主导（死在装甲波前）+ B.4 focused 停滞
- HEAD = f215c20

## 任务 1：拆分设计排期相容性评审（P1，立即——代码会话的文档在等你）

`design/spec/gamemanager-split-tech-design.md`（`f215c20`）过你这边的设计/排期视角：

1. 迁移顺序 S0-S7 与对账表排期建议的一致性（小批清障→主项→管理文档刷新）
2. R1-R8 风险登记里冻结期三项顺序裁定（数值外置先于 S4/S6、敌人池化插 S4/S5 之间）是否与你的排期意图吻合
3. 你关心的设计侧资产（数值表外置后的 schema 归属、波次状态机迁移后的可调参数面）在接缝清单里的位置
4. 结论回写文档附录或单独短评，通过则冻结期前置之二就绪

## 任务 2：帽后矩阵跟进（P0，等 QA 复跑结果——可能改变你的下一步）

- **达标** → 失衡诊断正式关账：报告补终版结论（配额修复 + 护甲帽双管齐下的最终画像），对账表最后一项闭环，周期终报供料
- **L13 仍死于早期波** → 启动你登记的下一侦查线：**波次数据的装甲波前置编排**（数据侧：L13 波次表中装甲波出现时机与玩家反制塔的解锁/经济时序匹配分析）。证据先行，产出最小改法（大概率是关卡数据调整而非代码）
- **focused 停滞** → B.4 插桩线（给 TryBuildP124Tower early-return 加计数插桩，QA 判据已挂）——插桩属自动化域，你实施、代码快评

## 任务 3：death 帧规格与代码挂钩的对齐确认（P2，随手）

代码会话已实施挂钩的排期（其 v8 任务 1）。挂钩落库后你抽查一次规格契约的实现一致性（本体卷轴 + 共享特效叠加 + 缺帧回退三点），有偏差开单。

## 任务 4：下周期规格管道（P2，看美术产能）

美术当前队列：T2 收口（4 帧）→ death 批次（48 张）→ Boss 立绘（需求已交付）→ 音频脚本。你的下一个规格需求视 T2/death 复验反馈决定：三档读感若 T2/T3 区分度不足，可能需要 T2 强化补丁规格；观感类问题优先参数化处置（本轮先例已两次验证）。

## 角色边界（延续）

管线不变。帽后矩阵结果若引发波次数据调整（L13 线）——关卡数据属设计侧资产，你的改动走数据文件，经代码快评或不需评审（视是否碰代码）。

## 验收标准（更新）

- 相容性评审结论落档（冻结期前置之二闭环）
- 帽后结果跟进：关账 or L13 波次侦查线启动
- death 挂钩一致性抽查完成
