# 设计会话后续计划（2026-08-21 14:40 总导演更新，v9）

## 进度快照（相对 v8）

- v8 各项在途：相容性评审（等做）、帽后矩阵跟进（等 QA 复跑）、death 挂钩抽查（等代码落地——其在途文件已见 TDEnemy 改动）
- **新需求到达（用户驱动，美术线发起）**：世界地图结构化重做（规格 v2 `159bafd` 已入库，美术批次约 30 张已排产）；其中**局外升级入口**暴露系统缺口——游戏无局外升级系统，美术只能备资产
- **总导演已批准局外升级系统立项**，规格由你出（见任务 1，插队为当前 P0）
- HEAD = 159bafd

## 🆕 任务 1：局外升级系统立项规格（P0，新项目线，立即启动）

产出 `design/spec/meta-upgrade-system-spec-v1.md`，四道护栏是验收线：

1. **MVP 克制**：升级树从几条有意义的升级起步（不是全树）——建议方向：局内经济/起始资源/编队槽位/重复可玩性侧，不做直接数值膨胀
2. **货币来源**：定义获取回路（如按局结算的余烬残渣/难度加成），与 p12.5.0 经济文档对齐或明确新增
3. **存档扩展**：campaign 进度存档的字段扩展方案（与 `TDCampaignProgression` 现有结构衔接）
4. **平衡红线（必写）**：平衡矩阵在 **meta-0 基线**下验证——元升级是重复可玩性加成，**不是通关必需**；L01-L20 的难度档位在零升级下必须自洽（刚为 L13/L20 付了两轮代价，这条不可让步）；升级效果上限设计成"爽感"不设计成"门槛"

另：入口交互流（worldmap 上的入口按钮 → 面板 → 升级反馈）画个流程图，与美术已备的入口/面板资产对齐（v2 规格里已列）。

完成后：总导演转代码会话排实现（独立文件红线已定）。

## 任务 2：worldmap 规格的信息设计复核（P1，美术第一波世界图验收时做）

美术按 v2 规格出世界图后，你从信息设计侧复核一次：

- 五区地貌与 20 关归属的映射正确性（campaign 数据对账）
- 三枚难度印章的 UI 语义（达成几档亮几枚——与 `highestDifficultyCleared` 三档对齐）
- 区域名牌、锁定/可挑战/已通关状态的光语言与参考图意图一致
- 局外升级入口的位置与视觉权重（为任务 1 的交互流服务）

## 任务 3：拆分设计排期相容性评审（P1，v8 遗留，尽快）

`gamemanager-split-tech-design.md` 的排期/风险裁定过设计视角，通过则冻结期前置之二闭环。**加一条新核对**：meta 升级系统与 worldmap 挂钩（~120-150 行 TDWorldMap 侧改动）是否影响 S 阶段排序——两个新功能线都应在拆分启动前落库或明确隔离。

## 任务 4：帽后矩阵跟进（P0，等 QA——判据树不变）

达标→失衡诊断关账；L13 仍死于早期波→波次数据装甲前置侦查线；focused 停滞→B.4 插桩。与任务 1 并行不冲突（一个等数据一个出规格）。

## 任务 5：death 挂钩一致性抽查（P2，等代码提交后）

三点抽查：本体卷轴播放、共享特效叠加、缺帧回退。顺手看 FrostCoil 文案落码与你附录 C.4 的一致性。

## 验收标准（更新）

- meta 升级系统规格交付（四道护栏齐，尤以平衡红线）
- worldmap 信息设计复核结论落档
- 相容性评审通过（含新功能线隔离核对）
- 帽后结果按判据树走线
