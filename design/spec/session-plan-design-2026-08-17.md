# 设计会话后续计划（2026-08-20 12:45 总导演更新，v7）

## 验收快照（相对 v6）

- ✅ 任务 1 配额定位：三问三答定案**条件缺陷**——配额代码活着，但 SiegeDrill 优先级第 7 进不了四槽编队，`Contains(SiegeDrill)` 恒 false；压力谓词成立非瓶颈；护甲帽按导演指令继续封存（附录 2 随 `72f46a6` 入库）
- ✅ 任务 2 修复实施：装甲感知编队（整关护甲压力主导时末槽换入 SiegeDrill），经代码评审落库 `4bb5480`
- ✅ 任务 3 FrostCoil 判定：维持现状（首发互锁是 R1 刻意设计）；闪避公式入 GDD；教学文案任务已登记
- ✅ 任务 4 文档批清零：对账表 B 类 6 项全关账（`72f46a6`）
- 发现并归档 B.4 次级异常（L13/L20 全部局建塔循环 3-5 塔后停滞、死亡时未花预算 348-1254），插桩路径已留给 QA 判据
- HEAD = 8a1dd56。**QA 手柄终验工件已产出（纯手柄主动开波 20/20），总导演现转交你回填 P12.3。**

## 任务 1：P12.3 手柄矩阵终态回填（P0，立即——输入已就位，无阻塞）

QA 工件三件（胜利画面 PNG / victory=PASS JSON / 逐波日志，`57af610`）+ 输入面 Phase A 19/19 + 三个 TD-GP 修复复验 + 终验文档。把这些落进你留好的占位：

- 16/16 手柄矩阵逐项终态（含 TD-GP-001/002/003/004 修复轨迹与提交号）
- 终验工件为注脚（环境：编辑器 + TD_AUTOMATION，被测手柄路径与发布版一致——QA 已注明）
- 至此 **P12.3 文档与实况零偏差**，对账表"等 QA 2 项"之一闭环

## 任务 2：失衡诊断关账（P0，等 QA 矩阵复跑结果——可能就是今天）

QA 复跑（多种子，编队判据 + B.4 分流）结果到达后：

- 达标（L13/L20 回档 + SiegeDrill 上场）→ 诊断报告补终版结论，失衡诊断正式关账，对账表最后一项闭环
- focused 停滞 → 走你留的 B.4 插桩路径（给 TryBuildP124Tower 的 early-return 加计数插桩），这是下一个侦查项
- 不达标 → 护甲帽解封，你实施（公式已预写）、代码评审（单测要求已挂其计划）

## 任务 3：敌人 death 帧规格（P1，提前启动——美术生图被网络阻塞，正好把规格备好）

原计划"等 T2 交付后"，但美术 T2 批次被 DNS 污染阻塞（等用户开代理），你把 death 帧规格**提前做出**，网络恢复后美术就有两批可跑（T2 优先、death 接续）。100 分路线图留的口，参考 fire/T2 规格格式（命名 + 生效机制 + 数量表）。注意与代码确认 death 帧的生效链路是否像 fire 帧一样"放入即播"，若需要挂钩在规格里明示。

## 任务 4：教学文案补丁（P2，你登记的任务）

FrostCoil 判定"维持现状"的配套：L03 教程/图鉴注明闪避机制（快速敌人对慢速单体塔的闪避 + 减速去闪避的反制教学）。文案改动直接做，走文档提交。

## 任务 5：Boss 立绘需求定义（P2，排产前置）

美术 backlog 的下一大项，出需求定义（尺寸/风格/用途/数量），供网络恢复后排产。

## 验收标准（更新）

- P12.3 终态回填完成，文档与实况零偏差
- 失衡诊断按 QA 结果关账或进入 B.4 插桩线
- death 帧规格交付、教学文案补丁落档、Boss 立绘需求定义交付
