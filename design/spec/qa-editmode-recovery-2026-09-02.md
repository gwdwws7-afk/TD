# 跑测器卡死恢复 + EditMode 全量复跑(计划 v18 任务 1+4,2026-09-02)

## 事故与恢复(运维档案)

- **现象**:代码会话跑 EditMode 即败卡死(域重载期间跑测器死锁);MCP 桥接 8080 连接拒绝(插件 HTTP 监听线程随卡死一并死亡),编辑器主进程存活。
- **恢复**:播放模式循环复位法不可达(无 MCP)→ **编辑器全重启**:优雅关闭(脚本树已干净、ProjectSettings 先还原)→ `D:\unity\2022.3.12f1\Editor\Unity.exe -projectPath E:\TD` → 监听 ~2 分钟内复活(PID 6136)→ 全新握手 → isCompiling=false 后跑测。
- **结论入 checklist**:桥接死亡级(端口无监听)= 编辑器重启,唯一解;冻结期老病的三级恢复梯度更新为 ①重跑 EditMode 循环(MCP 可达时)②插件 WebSocket 自愈 ③编辑器重启(本轮实证,~5 分钟回线)。

## EditMode 全量复跑 ✅ 86/86

- 覆盖 d01bf04(C-2 帧表扩容)/e4245ff(C-2 行为特效)→ 5b751db/9fb2372/5988265(批 3 重织+B.4 修复)→ d5edd22 全段落库后状态(HEAD)
- 基线 86/86 保持;**批 3 未新增 EditMode 用例**(重织为数据面)——"+新增"预期为零属正常结果,如实记录
- 测试后播放模式健康(frame 273→561,无卡死复发)
- 控制台仅 MCP 插件自身 WebSocket 重连噪音(既有现象,非项目错误)

## 附:任务 4 ProjectSettings 还原 ✅(本轮开头已执行)

## 批 4 待命

三难度腿扩充前基线齐备(Standard 12/20 / Veteran 11/20 / EmberTrial 12/20);工具 `qa_expansion_matrix.py` 就绪;**等批 3 重织的验收放行信号即开跑 20 关 × 3 难度 × 3 策略全矩阵**。
