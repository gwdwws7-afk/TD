# 美术会话后续计划（2026-08-24 14:35 总导演更新，v10）

## 进度快照（相对 v9）

- ✅ 内容扩充设计文档全部就绪——你的排产规格从"够用"变成"全套"：四新塔数值与外观定位、18+5 敌投放表、4 Boss 相位规格、轨障车行为
- 工作区在途：敌人 death 帧（多敌种已生成待验收提交）、编队面板 UI 资产（Formation/）
- worldmap 已接线（`6b61a39`，含性能优化）；T2 批次状态自查（v9 的 4 缺帧若仍开着先补）
- HEAD = 32cf33c

## 任务 1：收口在途批次（P0，先把手上的做完）

1. **敌人 death 帧批次**（12 敌 × 4 帧规格）：全量自验（本体一致性 + 连通域 + 目检）→ 一笔提交 → 通知总导演转 QA 实机复验（死亡卷轴 + 尸体染色修复一起看）
2. **编队面板 UI 资产**：按 `4b7cc20` 规格与三轮评审修正（`c3b4641`/`15f3b34` 等）验收提交
3. T2 4 缺帧若仍开：同趟生图机补齐收口

## 任务 2：内容扩充大批量（P0，任务 1 后的主战场——设计已宣布你可全量排产）

按设计排产表的优先顺序（细节以规格为准）：

1. **四新塔全套**（喷灯 L04/吊机 L09/轨障车 L14/轨道炮 L18）：idle + fire + T2 全档，命名与生效链路沿用现有塔模式（fire/T2 放入即播，后处理器已覆盖）
2. **Boss 帧**（4 Boss × 相位规格——装卸兽/岔道暴君/窑炉看守/回响先驱，每只含相位切换的表现差异帧）
3. **六新敌活体帧**（投放矩阵 v2 的六新敌基线——视觉规格 `expansion-visual-identities-v1.md` 已含剪影与帧数清单，提示词已备）
4. Boss 立绘（需求 `boss-portrait-requirements-v1.md`：3 件产物/双相位可视编码）

**生成入口已全量就绪**（`e3a8270`）：`tools/generate_expansion.py` 覆盖 334 资产（塔 107/敌 108/Boss 104/立绘 15）带逐帧依赖链，提示词四件套共 2,282 行——按 T2 批次同款模式（断点续跑/分塔执行）驱动即可，先小样后全量的纪律不变。

**分批交付纪律**：每批独立提交 + 自验报告，别攒大包；首批（建议四新塔的 idle 帧）先出小样给设计确认风格锚定，再放全量——T2 批次"先小样后全量"的先例省了大量返工。

## 任务 3：v7 遗留本地任务（P2，生成等待期穿插）

音频导入脚本已在代码会话加固过（`7453572` 三处加固）——核对它是否已覆盖你原任务的范围，覆盖则销项；假 PNG logo 维持 backlog。

## 任务 4：worldmap 第二波（P2，若第一波尚未收口）

地标 20 张 + 徽章/印章/名牌/入口——按 v2 规格顺序，第一波验收门禁过了才放。

## 协作注意（延续）

- 生图机 pull/push 纪律；大批量期间更严格（多机漂移风险随批量上升）
- 设计的规格答疑通道已开（其 v10 任务 5）——结构性歧义问设计，观感类参数化自处
- QA 停摆中：你的 death 帧实机复验会排队，别等它——按流程提交，复验后补

## 验收标准（更新）

- death 批 + 编队面板 + T2 收口三笔落地
- 四新塔首批小样过设计确认 → 全量批次按序交付
- 每批独立提交 + 自验报告，工作区不积压
