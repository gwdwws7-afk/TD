# 美术会话后续计划（2026-08-21 14:40 总导演更新，v9）

## 进度快照（相对 v8）

- ✅ 用户参考图分析 + 两轮结构纠偏落库：规格 v1.1（`bca8a04`）→ **结构版 v2**（`159bafd`，`campaign-worldmap-art-spec-v2.md`）+ 导入地基（`d46e064`）
- ✅ 数据侧自查：5 地貌分区（20 关 ÷ 4）、三档难度印章数据现成、局外升级系统不存在（已上报立项）
- v8 任务不变项：T2 仍缺 4 帧（`ember_flak_t2_02`、`grav_snare_t2_04/05`、`rail_lancer_t2_03`）
- **总导演已批准 worldmap v2 扩大版与排期**（见任务 2）；death 帧与 Boss 立绘顺延
- HEAD = 159bafd

## 任务 1：补齐 T2 最后 4 帧（P0，第一优先——与 worldmap 同趟生图机）

流程同 v8：pull → `generate_tower_t2.py`（断点续跑）→ 48 帧全量自验 → 一笔提交 → 转总导演安排 QA 复验。

## 任务 2：世界地图批次（P0，用户驱动的新主项——T2 之后立即）

按 v2 规格，**严格分两波交付**：

**第一波（先出，验收门禁）**：世界图 1 张。地标要贴着它的地貌画，世界图定了地貌基调才能批量地标——这一张过了验收再放第二波。

**第二波（第一波验收后）**：地标 20 张（`landmark_L01..L20`，同区互异共享地貌材质）→ 徽章/印章 7 → 名牌/入口/面板 4（含局外升级入口+面板框体，系统本体在设计与代码线）→ 发光轨道条 1。

验收流程沿用：数值自检 → 目检 → 提交 → 转总导演。世界图验收时重点核对：五区地貌与现有 5 张 4096 地表同语言、主铁路动线贯穿、四角压暗中央暖光。

## 任务 3：death 帧批次（P1，顺延到 worldmap 之后）

规格已就位（12 敌 × 4 帧），代码挂钩在代码会话 v8/v9 线上。worldmap 两波之间若有生图机空档可穿插，但不抢世界图验收节奏。

## 任务 4：v7 遗留本地任务（P2，不变）

音频导入脚本 + 假 PNG logo——生成等待期做。

## 任务 5：backlog（P2，顺延后排）

Boss 立绘（需求已交付）→ 视复验反馈的补丁规格。

## 协作注意（延续）

- 生图机同步纪律：开工 pull、完工 push
- 代码会话在途文件（TDEnemy/TDGameManager/TDTowerTooltip）别碰
- worldmap 代码挂钩（~120-150 行）归代码会话，契约在规格里——你只管资产

## 验收标准（更新）

- T2 48/48 入库收口
- 世界图第一波验收通过 → 地标等第二波全量交付（约 30 张整体关账）
- 局外升级入口/面板资产随批交付（系统本体另行关账）
