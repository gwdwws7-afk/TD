# 美术会话后续计划（2026-08-27 23:25 总导演更新，v14）

## 进度快照（相对 v13）——Phase A/B 收口，推送清偿

- ✅ 占位图修复入库（`4e99ce1`+`04fd7ac`，bbox 按 7 月原脚印回缩）——QA 实机复看过，用户圈选问题关账
- ✅ redo 批 8/11 代切入库（`ccb7f6d`）+ 轨障车 t2_00/01 本体合成补交（`a2e5ab2`）；4 张退回重排队
- ✅ **151 个积压提交全部推送完毕**（github 恢复）——单点风险解除
- ✅ MiniMax 下一段指令已备好（Phase C 44 缺帧 + 4 张退回重做，可直接粘贴）
- 设计 C-1 裁定：三塔放行；**rail_barricade 本地调色**（警戒钢蓝回主装甲色，禁重生成）
- HEAD = 2197a95

## 任务 1：rail_barricade 本地调色（P0，最先做——它堵着 18 张帧的链）

按设计裁定（`8858544`）：主装甲板基色回钢蓝 #5D8AA8（允许 #46697F~#7FA8C4 体积渐变，锈蚀降为 ≤15% 点缀），**只动色相层不碰剪影**，本地调色通道处理、禁止生图机重生成。已入库的 barricade 帧 + 后续 11 缺帧 + 7 重做帧全部按新口径。完成后通知设计复检（其 v14 任务 1）。

## 任务 2：Phase C + 4 张退回重做（P0，MiniMax 续跑——指令已备）

你报告里那段可粘贴指令发给用户转 MiniMax：44 张缺帧（三塔立即；barricade 的 11 张等调色落库后按新底色跑）+ 4 张退回（火焰层收束×2、超宽出框×1、家族构图反链×1）。到图走老管线（数值审计 → t2 本体合成 → 目检 → 提交）。C-1 收口 = 96/96。

## 任务 3：C-2 敌 108 批（P1，C-1 关账后）

开跑前建议沿用"先小样后全量"：先出 2-3 个敌种的完整帧组给设计锚定风格（其 v14 任务 3 会接），过了再放全量。

## 任务 4：backlog（P2，不变）

假 PNG logo；C-3 Boss 104 与立绘 15 排在 C-2 后。

## 验收标准（更新）

- barricade 调色落库 + 设计复检过 → 18 张帧链解锁
- C-1 96/96 关账；C-2 小样 → 全量按批推进
- 每批独立提交 + 自验报告
