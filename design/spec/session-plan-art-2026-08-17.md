# 美术会话后续计划（2026-08-27 21:40 总导演更新，v13）

## 进度快照（相对 v12）

- ✅ C-1 链帧第一批 52/96 入库（`088a2b1`，含雾清理与 T2 重建）
- 🔶 **占位图重制两张刚落盘**（build_marker / tower_base_plate，08-27 21:34 工作区）——待你验收提交
- ⏸️ 生图机 08-25 晚停机两天（外部 worker，用户驱动；指令文档已备 `minimax_expansion_towers.txt` 落盘纪律节 + `minimax_placeholder_redo.txt`）
- ⚠️ github 不通，151 个本地提交积压
- 缺口：C-1 余 44 帧 + 11 张出框重做；C-2 敌 108 / C-3 Boss 104 / 立绘 15 未开跑
- 设计/QA/代码三个会话自 08-24/25 起停摆（总导演正在重新动员）

## 任务 1：占位图验收提交（P0，立即——图刚到）

按 `1e08e46` 规格验收两张：数值自检（尺寸/透明/实体率）+ 目检（正八边形锻铁底盘+铆钉圈+炉口琥珀微光 / 锻铁空心环+十字定位栓）→ 提交（保留现有 .meta）→ 通知总导演两件事：①代码会话执行 baseTint→白 配套（其计划已挂）；②安排实机复看（你圈的灰盘问题关账）。

## 任务 2：C-1 收口（P0，生图机续跑后）

- **11 张出框重做帧**：等设计 C-1 结论（其 v13 任务 2）再放——出框重做若风格结论有调整，先裁再跑避免二次返工
- **44 张缺口帧**：设计结论无调整则直接按 `generate_expansion.py --import-only --batch towers` 续跑补齐
- 到图后走既有验收管线（数值审计 → t2 本体重建 → 目检 → 提交），52+44+11 = 96 帧 C-1 关账
- **定稿保护条款继续有效**：已验收提交的帧一律不碰

## 任务 3：C-2 / C-3 / 立绘排队（P1，C-1 关账后）

敌 108 → Boss 104 → 立绘 15，提示词与依赖链全备，按生图机产能推进，分批提交纪律不变。

## 任务 4：push 积压（P0，网络恢复第一件事）

151 个提交单点风险。若持续不通，做 `git bundle` 到另一块盘的临时备份（一条命令，让总导演知道备份路径即可）。

## 任务 5：工作区卫生（P2，顺手）

`_audit/`、`_audit_shot.jpg` 是 QA 取证散件——提醒 QA 清理，不属你的提交范围。

## 验收标准（更新）

- 占位图两张验收提交 + 实机复看通过 → 灰盘问题关账
- C-1 96 帧全量关账（重做帧经设计结论裁后放行）
- C-2 起按批推进；push 或 bundle 备份落地
