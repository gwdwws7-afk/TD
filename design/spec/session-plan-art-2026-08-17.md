# 美术会话后续计划（2026-08-20 12:45 总导演更新，v7）

## 验收快照（相对 v6）

- ✅ T2 工程全部就绪：一键驱动 `generate_tower_t2.py`（`4bc0286`，逐帧底图红线已机制化、断点续跑、--kinds 分塔、全链串联）+ 后处理器覆盖 + 本体保护合成器
- 🔴 **T2 生图被网络硬阻塞（DNS 污染，等用户开代理）**——这是唯一等待项，解法见下
- HEAD = 8a1dd56

## 🔴 阻塞解除方法（需要用户操作，你确认环境后执行）

```bash
export HTTPS_PROXY=http://127.0.0.1:<代理端口>
cd E:/TD
python tools/generate_tower_t2.py            # 48 帧一键全流程
```

代理开启后先跑 `--dry-run` 或单塔 `--kinds rail_lancer` 验证连通，再放全量。SDK 与 key 读取已就位，无需改动。

## 任务 1：T2 批次 48 张（P0，网络恢复后立即——保持最高优先级）

流程不变：生成 → 透明化 → 本体保护合成（模块层 >45% 警告帧单独 `--force` 重跑）→ 自验 → 提交 → 转总导演安排 QA 三档读感复验。

## 任务 2：网络阻塞期的本地工作（P1，现在就做——不依赖生图 API）

1. **音频导入脚本**——backlog 里"美术侧收口后动"的条件已满足（缺口清零），`design/spec/audio-design-spec-v1.md` 底稿已有。参考 `TDArtBatch103Import.cs` 模式：OnPreprocessTexture 同款思路做 AudioClip 导入规整（压缩格式/加载类型分平台），纯本地工程活
2. **假 PNG logo 修复**——既定 backlog 维持项，同样纯本地
3. 两项各自独立提交，代码侧评审走代码会话快验线

## 任务 3：backlog 排队（P2，网络恢复后按序）

1. T2 批次（任务 1）
2. 敌人 death 帧——设计正在提前出规格（其 v7 任务 3），到手即可排产
3. Boss 立绘——设计的需求定义在路上（其 v7 任务 5）

## 协作注意

- 网络问题是你机器环境层面的（DNS 污染 + 无代理），会话内无法自救——每次开工前若仍不通，直接报告等待，别反复诊断浪费时间（诊断链已完整留档）
- 平衡侧（QA 矩阵、可能的护甲帽）与你无冲突

## 验收标准（更新）

- 网络恢复后 T2 48 帧一键流程跑通、合成自验、提交、QA 复验关账
- 阻塞期完成音频导入脚本 + 假 PNG 修复两笔本地提交
- 之后按 death 帧 → Boss 立绘排队（设计规格前置）
